let options = {
  width: 1024,
  heigh: 1024,
  name: "test"
};

//console.log(options.name);
options.bool = false;

options.bool = true;
options.width = 1920;
options.heigh = 1080;

options.num = 31;
console.log(options);
